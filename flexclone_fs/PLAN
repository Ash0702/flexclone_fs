Right now there is no block specifications , the whole file will be see through.

How to do ????
Changes in Inode (struct ext4_inode_info , not struct ext4_inode) (Not the on disk one , only how kernel represents it).
--> Adding struct flexclone_info {unsinged long parent_inode , struct inode* parent , bool is_par}
--> struct ext4_inode_info{
	...
	struct fleclone_info; //we will add this
	...
	}


-->For this I'm using xattrs (which are stored as seperate thing on the disk). the kernel always loads struct ext4_inode_info using ext4_iget( some funcn like this ) and we will change that part to correctly load ext4_inode_info.
 
-->The user will provide us parent and child info using ioctl. 
	int par = open("par");
	int child = open("child");
	ioctl(par , child , CREATE); //psuedocode

-->Then we will try to change how write functions

--> user will do updates via ioctl
	int chld = open("child");
	ioctl(chld , UPDATE)
